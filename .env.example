# Copy to .env (git-ignored) and fill in real values before running `docker compose up`.
# Used by docker-compose.yml only — application secrets live in
# backend/src/Korner.Api/appsettings.Local.json (see docs/SECRETS.md).

MSSQL_SA_PASSWORD=change-me-strong-password
